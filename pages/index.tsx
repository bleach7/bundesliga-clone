import { HomeContent } from "@/components";
import { PublicLayout } from "@/layouts";

const Home = () => {
  return (
    <PublicLayout>
      <HomeContent />
    </PublicLayout>
  );
};

export default Home;
